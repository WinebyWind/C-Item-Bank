#include<stdio.h>
int main()
{   
  double s1=0,s2=0,s,i;
  for(i=0;i<10000;i++){
	  if(1/(4*i+1)<0.00001)break;
	    s1=s1+1/(4*i+1);
	  if(1/(4*i+3)<0.00001)break;
      s2=s2-1/(4*i+3);
  }
	s=s1+s2;
  printf("%lf\n",s);
  return 0;
}
